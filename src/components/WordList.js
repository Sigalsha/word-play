import React from "react";

const WordList = () => {
  return (
    <div className="word-list">
      {/*       <ul>
        {words.map((words) => {
          return <Card word={word} key={word.id} isSubjectCard={false} />;
        })}
      </ul> */}
    </div>
  );
};

export default WordList;
